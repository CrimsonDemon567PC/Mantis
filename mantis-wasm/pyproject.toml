[project]
name = "mantis"
version = "1.0.0"
description = "Mantis compiler and loader for WebAssembly"
authors = [{ name = "Vincent Noll" }]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pyodide]
packages = ["mantis"]
